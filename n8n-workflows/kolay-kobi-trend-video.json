{
  "name": "Kolay KOBİ — Trend Video Bulucu",
  "nodes": [
    {
      "parameters": {
        "httpMethod": "POST",
        "path": "kolay-kobi-trend-video",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "webhook-node",
      "name": "Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 300],
      "webhookId": "kolay-kobi-trend-video",
      "disabled": false
    },
    {
      "parameters": {
        "jsCode": "// ─── İKİ KATMANLI LİMİT ─────────────────────────────────────────────────\n// 1) IP başına GÜNLÜK limit: tek kullanıcının sistemi spam'lemesini engeller.\n// 2) GLOBAL AYLIK limit: Apify TikTok Trending çalıştırması başına ~$0.10\n//    (20 sonuç, TR proxy filtresi dahil) — TÜM kullanıcılar arasında\n//    paylaşılan gerçek bütçeyi korur. IP limiti bunu KORUMAZ — 50 farklı\n//    kullanıcı günde 1'er istek atsa bile toplam kota hızla biter. Apify'in\n//    aylık $5 ücretsiz kredisi ~50 çalıştırmaya denk geldiği için, pay\n//    bırakmak için 50 değil 45'te kesiyoruz.\nconst PER_IP_DAILY_LIMIT = 50;\n// NOT: 'Build Search Query' 2 yerine 4 hashtag kullanmaya başladı (fyp,\n// keşfet, turkiye, trendturkiye) — Apify muhtemelen her hashtag için ayrı\n// ücretlendiriyor, yani çalıştırma başına maliyet ~2 katına çıkabilir. İlk\n// gerçek test sonrası Apify Storage/Usage sayfasından gerçek maliyeti\n// kontrol edip bu sayıyı ona göre ayarla — şimdilik ihtiyatlı başlangıç.\nconst GLOBAL_MONTHLY_SEARCH_LIMIT = 25;\n\nconst ip = $input.first().json.headers?.['x-forwarded-for']?.split(',')[0]?.trim() || $input.first().json.headers?.['x-real-ip'] || 'unknown';\nconst today = new Date().toISOString().slice(0, 10);\nconst monthKey = today.slice(0, 7); // 'YYYY-MM'\nconst ipKey = 'rl_trend_' + ip + '_' + today;\nconst globalKey = 'rl_trend_global_' + monthKey;\n\nconst staticData = $getWorkflowStaticData('global');\nif (!staticData.rateLimits) staticData.rateLimits = {};\n\nconst ipCount = staticData.rateLimits[ipKey] || 0;\nconst globalCount = staticData.rateLimits[globalKey] || 0;\n\nif (ipCount >= PER_IP_DAILY_LIMIT) {\n  return [{ json: { rateLimitExceeded: true, reason: 'ip_daily' } }];\n}\nif (globalCount >= GLOBAL_MONTHLY_SEARCH_LIMIT) {\n  return [{ json: { rateLimitExceeded: true, reason: 'global_monthly' } }];\n}\n\nstaticData.rateLimits[ipKey] = ipCount + 1;\nstaticData.rateLimits[globalKey] = globalCount + 1;\n\nconst body = $input.first().json.body || $input.first().json;\nreturn [{ json: { rateLimitExceeded: false, ...body } }];"
      },
      "id": "rate-limit-node",
      "name": "Rate Limit Check",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 300],
      "disabled": false
    },
    {
      "parameters": {
        "conditions": {
          "options": {
            "caseSensitive": true,
            "leftValue": "",
            "typeValidation": "strict"
          },
          "conditions": [
            {
              "id": "c1",
              "leftValue": "={{ $json.rateLimitExceeded }}",
              "rightValue": true,
              "operator": {
                "type": "boolean",
                "operation": "equal"
              }
            }
          ],
          "combinator": "and"
        }
      },
      "id": "if-rate-limited",
      "name": "Rate Limited?",
      "type": "n8n-nodes-base.if",
      "typeVersion": 2,
      "position": [680, 300],
      "disabled": false
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ error: \"rate_limit\", reason: $json.reason || \"unknown\" }) }}",
        "options": {
          "responseCode": 429
        }
      },
      "id": "respond-rate-limit",
      "name": "429 Response",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [900, 180],
      "disabled": false
    },
    {
      "parameters": {
        "jsCode": "// Webhook zaten 'Respond With Job ID' ile cevap verdi; burada tekrar\n// HTTP yanıtı DÖNDÜRMÜYORUZ, sadece sonucu static data'ya job_id altında yazıyoruz.\nconst staticData = $getWorkflowStaticData('global');\nconst jobId = $('Create Job').first().json.jobId;\nconst payload = { ...$json };\n\nif (staticData.jobs && staticData.jobs[jobId]) {\n  staticData.jobs[jobId] = { status: 'completed', createdAt: staticData.jobs[jobId].createdAt, ...payload };\n}\nreturn [{ json: payload }];"
      },
      "id": "respond-success",
      "name": "Store Job Result",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [2000, 400],
      "disabled": false
    },
    {
      "parameters": {
        "jsCode": "const data = $input.first().json;\nconst staticData = $getWorkflowStaticData('global');\nif (!staticData.jobs) staticData.jobs = {};\n// 30 dakikadan eski job kayıtlarını temizle (static data şişmesin)\nconst now = Date.now();\nObject.keys(staticData.jobs).forEach(id => {\n  if (now - (staticData.jobs[id].createdAt || 0) > 30 * 60 * 1000) delete staticData.jobs[id];\n});\nconst jobId = now.toString(36) + Math.random().toString(36).slice(2, 10);\nstaticData.jobs[jobId] = { status: 'pending', createdAt: now };\nreturn [{ json: { ...data, jobId } }];"
      },
      "id": "create-job-node",
      "name": "Create Job",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [900, 260],
      "disabled": false
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify({ job_id: $json.jobId, status: 'pending' }) }}",
        "options": {
          "responseCode": 200,
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "application/json; charset=utf-8"
              }
            ]
          }
        }
      },
      "id": "respond-job-id-node",
      "name": "Respond With Job ID",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [1120, 260],
      "disabled": false
    },
    {
      "parameters": {
        "jsCode": "// Apify çağrısı başarısız olduğunda job'ı 'error' olarak işaretle.\n// HTTP yanıtı zaten 202 + job_id olarak gönderildi; buradan yeni yanıt DÖNMÜYORUZ.\n// İstemci polling'de status:'error' görünce kullanıcıya hata mesajı gösterir.\nfunction extractMessage(e) {\n  if (!e) return 'Bilinmeyen hata';\n  if (typeof e === 'string') return e;\n  if (typeof e.message === 'string' && e.message) return e.message;\n  if (typeof e.error === 'string' && e.error) return e.error;\n  if (e.error && typeof e.error === 'object') {\n    if (typeof e.error.message === 'string' && e.error.message) return e.error.message;\n    try { return JSON.stringify(e.error).slice(0, 400); } catch (_) {}\n  }\n  if (typeof e.statusCode !== 'undefined') return `HTTP ${e.statusCode}: ${JSON.stringify(e).slice(0, 300)}`;\n  try { return JSON.stringify(e).slice(0, 400); } catch (_) { return 'Bilinmeyen hata'; }\n}\n\nconst staticData = $getWorkflowStaticData('global');\nconst jobId = $('Create Job').first().json.jobId;\nconst err = $input.first().json;\nconst errorMessage = extractMessage(err);\n\nif (staticData.jobs && staticData.jobs[jobId]) {\n  staticData.jobs[jobId] = {\n    status: 'error',\n    createdAt: staticData.jobs[jobId].createdAt,\n    error: errorMessage\n  };\n}\nreturn [{ json: { ok: false, error: errorMessage } }];"
      },
      "id": "mark-job-error-node",
      "name": "Mark Job Error",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1780, 500],
      "disabled": false
    },
    {
      "parameters": {
        "httpMethod": "GET",
        "path": "kolay-kobi-trend-video-status",
        "responseMode": "responseNode",
        "options": {}
      },
      "id": "status-webhook-node",
      "name": "Status Webhook",
      "type": "n8n-nodes-base.webhook",
      "typeVersion": 2,
      "position": [240, 700],
      "webhookId": "kolay-kobi-trend-video-status",
      "disabled": false
    },
    {
      "parameters": {
        "jsCode": "const q = $input.first().json.query || {};\nconst jobId = q.jobId;\nconst staticData = $getWorkflowStaticData('global');\nconst job = (staticData.jobs || {})[jobId];\nif (!job) return [{ json: { status: 'not_found' } }];\nreturn [{ json: job }];"
      },
      "id": "read-job-status-node",
      "name": "Read Job Status",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [460, 700],
      "disabled": false
    },
    {
      "parameters": {
        "respondWith": "json",
        "responseBody": "={{ JSON.stringify($json) }}",
        "options": {
          "responseCode": 200,
          "responseHeaders": {
            "entries": [
              {
                "name": "Content-Type",
                "value": "application/json; charset=utf-8"
              }
            ]
          }
        }
      },
      "id": "status-response-node",
      "name": "Status Response",
      "type": "n8n-nodes-base.respondToWebhook",
      "typeVersion": 1,
      "position": [680, 700],
      "disabled": false
    },
    {
      "parameters": {
        "jsCode": "// Kullanıcının isteği: sektöre özel arama DEĞİL, genel olarak GERÇEKTEN\n// trend olan (yüksek izlenmeli) videolar. Bunlar sonradan MiniMax tarafından\n// sektöre uyarlanacak (penguen → lojistik mantığı). Bu yüzden sabit,\n// geniş kapsamlı trend hashtag'leri kullanıyoruz, sektöre göre değil.\nconst hashtags = ['fyp', 'keşfet', 'turkiye', 'trendturkiye'];\nconst data = $input.first().json;\nreturn [{ json: { ...data, hashtags } }];"
      },
      "id": "build-search-query-node",
      "name": "Build Search Query",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1340, 260],
      "disabled": false
    },
    {
      "parameters": {
        "method": "POST",
        "url": "https://api.apify.com/v2/acts/clockworks~tiktok-scraper/run-sync-get-dataset-items",
        "sendQuery": true,
        "queryParameters": {
          "parameters": [
            {
              "name": "token",
              "value": "BURAYA_GERCEK_APIFY_TOKEN_YAZ"
            }
          ]
        },
        "sendBody": true,
        "specifyBody": "json",
        "jsonBody": "={{ JSON.stringify({\n  hashtags: $json.hashtags,\n  resultsPerPage: 20,\n  proxyCountryCode: 'TR',\n  commentsPerPost: 0,\n  topLevelCommentsPerPost: 0,\n  maxRepliesPerComment: 0,\n  maxFollowersPerProfile: 0,\n  maxFollowingPerProfile: 0,\n  excludePinnedPosts: false,\n  scrapeAdditionalAuthorMeta: false,\n  scrapeRelatedSearchWords: false,\n  scrapeRelatedVideos: false,\n  shouldDownloadAvatars: false,\n  shouldDownloadCovers: false,\n  shouldDownloadMusicCovers: false,\n  shouldDownloadSlideshowImages: false,\n  shouldDownloadVideos: false\n}) }}",
        "options": {
          "timeout": 120000
        }
      },
      "id": "apify-tiktok-trending-node",
      "name": "Apify TikTok Trending",
      "type": "n8n-nodes-base.httpRequest",
      "typeVersion": 4.2,
      "position": [1560, 260],
      "onError": "continueErrorOutput",
      "disabled": false
    },
    {
      "parameters": {
        "jsCode": "const ctx = $('Build Search Query').first().json;\nconst items = $input.all().map(i => i.json);\nconst isError = items.length === 1 && items[0] && items[0].error;\nconst rawVideos = (!isError && items.length) ? items : [];\n\nconst videos = rawVideos\n  .sort((a, b) => (b.playCount || 0) - (a.playCount || 0))\n  .slice(0, 12)\n  .map((v, i) => ({\n    rank: i + 1,\n    title: (v.text || '').slice(0, 150),\n    platform: 'TikTok',\n    author: v.authorMeta?.name || '',\n    authorNickname: v.authorMeta?.nickName || '',\n    playCount: v.playCount || 0,\n    diggCount: v.diggCount || 0,\n    commentCount: v.commentCount || 0,\n    shareCount: v.shareCount || 0,\n    webVideoUrl: v.webVideoUrl || '',\n    hashtags: (v.hashtags || []).map(h => h.name || h).slice(0, 6),\n    musicName: v.musicMeta?.musicName || '',\n  }));\n\nconst resultData = {\n  status: 'completed',\n  videos,\n  sector: ctx.sector || '',\n  totalFound: rawVideos.length,\n};\nreturn [{ json: resultData }];"
      },
      "id": "merge-search-results-node",
      "name": "Format Results",
      "type": "n8n-nodes-base.code",
      "typeVersion": 2,
      "position": [1780, 260],
      "disabled": false
    }
  ],
  "connections": {
    "Webhook": {
      "main": [
        [{ "node": "Rate Limit Check", "type": "main", "index": 0 }]
      ]
    },
    "Rate Limit Check": {
      "main": [
        [{ "node": "Rate Limited?", "type": "main", "index": 0 }]
      ]
    },
    "Rate Limited?": {
      "main": [
        [{ "node": "429 Response",  "type": "main", "index": 0 }],
        [{ "node": "Create Job",    "type": "main", "index": 0 }]
      ]
    },
    "Create Job": {
      "main": [
        [{ "node": "Respond With Job ID", "type": "main", "index": 0 }]
      ]
    },
    "Respond With Job ID": {
      "main": [
        [{ "node": "Build Search Query", "type": "main", "index": 0 }]
      ]
    },
    "Build Search Query": {
      "main": [
        [{ "node": "Apify TikTok Trending", "type": "main", "index": 0 }]
      ]
    },
    "Apify TikTok Trending": {
      "main": [
        [{ "node": "Format Results",  "type": "main", "index": 0 }],
        [{ "node": "Mark Job Error",  "type": "main", "index": 0 }]
      ]
    },
    "Format Results": {
      "main": [
        [{ "node": "Store Job Result", "type": "main", "index": 0 }]
      ]
    },
    "Status Webhook": {
      "main": [
        [{ "node": "Read Job Status", "type": "main", "index": 0 }]
      ]
    },
    "Read Job Status": {
      "main": [
        [{ "node": "Status Response", "type": "main", "index": 0 }]
      ]
    }
  },
  "settings": {
    "executionOrder": "v1"
  },
  "tags": ["kolay-kobi", "apify", "trend-video"],
  "meta": {
    "instanceId": ""
  }
}
